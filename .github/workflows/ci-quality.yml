name: CI Quality Gate

on:
  pull_request:
  push:
    branches: ["main"]

jobs:
  quality:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v4

      - name: Setup pnpm
        uses: pnpm/action-setup@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: pnpm

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Report maintainability hotspots
        run: pnpm quality:hotspots

      - name: Run architecture verification
        run: |
          # includes pnpm lint
          # includes pnpm deps:check
          # includes pnpm build:web
          pnpm verify:architecture

      - name: Validate benchmark dataset
        run: pnpm bench:quality -- --dry-run

      - name: Install Playwright browser
        run: pnpm exec playwright install --with-deps chromium

      - name: Run E2E tests
        run: pnpm test:e2e
